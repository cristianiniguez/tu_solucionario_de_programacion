export type TSubject = {
  id: string;
  name: string;
  slug: string;
  description: string;
  brandColor: string;
  textColor: string;
  icon: string;
  postsDb: string;
};

export type TPost = {
  id: string;
  name: string;
  description: string;
  papersDb: string;
};
